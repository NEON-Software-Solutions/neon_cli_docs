---
next: /brick_setups/firebase_auth_setup
nextTitle: Firebase Auth Setup
---

# NEON CLI

Developed mit 💙 von NEON Software Solutions

---

Ein Command Line Interface für Dart, das nur ein Gas kennt.

## Installieren

Clone dir das Repo (am besten direkt in dein Root-Verzeichnis, damit es hier liegt: ```~/NEON_cli```, das erleichtert später den update Command). Jetzt musst du es nur noch global aktivieren:

```sh
dart pub global activate --source path pfad/zu/diesem/verzeichnis
```

Pro Tip: Falls du gerade im Verzeichnis bist, in das du das Repo geklont hast, dann funktioniert auch ein relativer Pfad:

```sh
dart pub global activate --source path ./NEON_cli
```

## Commands

### `NEON abfahrt`

Erstellt dir ein neues Flutter Projekt anhand der [NEON Core App][template_project_link]. Dauert ungefähr 3.5 Tornados 🍺

### `NEON build_flow`

Generiert dir den Boilerplate Code für ein neues Flow-Feature, gemäß der Logik des [Flow Builder][flow_builder_link] Packages.

### `NEON update - (in progress)`

Funktioniert leider noch nicht, da dart packages es nicht so geil finden, sich selbst zu deinstallieren... #unverständnis

Da unsere CLI nicht auf den pub.dev Servern liegt, können wir deren tolle Mechanismen nicht nutzen. Also ganz klare Nummer: jetzt wirds richtig dreckig 🐷

Hack fürs updaten:

1. CLI deaktivieren:

    ```sh
    dart pub global deactivate NEON_cli
    ```

2. In das CLI Repo navigieren (im Idealfall liegt das bei dir in ~/NEON_cli)
3. ```git pull``` durchzünden
4. CLI neu aktivieren:

    ```sh
    dart pub global activate --source path ./NEON_cli
    ```

5. CLI neu kompilieren: ```NEON``` (ja, wirklich nur das eintippen!)
6. Vollgas! 🏎

Eine kleine Zeitreise - sollte das Feature jemals funktionieren, ist das die Doku dazu:

Updated die NEON CLI. Übergib über das ```cli-path``` Flag den absoluten Pfad zu dem CLI-Ordner auf deinem Rechner, oder sei ein Highperformer und installiere die CLI initial in ```~/NEON_cli```, dann musst du gar kein Flag übergeben! 😤

```sh
🥵 Ein absolutes High-Performer-Tool, vom wilden Typen für wilde Typen.

Usage: NEON <command> [arguments]

Global options:
-h, --help       Print this usage information.
    --version    Einmal die aktuelle Version to go bitte.

Available commands:
  abfahrt   NEON abfahrt <projektname>
            Erstellt ein neues Projekt mit dem angegebenen Namen nach NEON Maßstäben (to the 🌝) im aktuellen Verzeichnis.
  build-flow   NEON build-flow <flowname>
               Generiert den Boilerplate Code für ein Flow Feature
```

#### Wie funktioniert's?

```sh
# Erstelle eine neue App, die geile_app heißt:
NEON abfahrt geile_app
```

```sh
# Lass dir den Boilerplate-Code für ein Flow-Feature generieren:
NEON build_flow mein_flow
```

#### Was lass ich mich hier zwischen die Drehscheiben gravieren?

Alles, was das Template Project drauf hat, findest du im [README des Projektes][template_project_link], unter "What's in the box?"

### `NEON --help`

Für die Hilflosen unter uns.

### Maintenance

Solltest du mal zu den glücklichen Seelen gehören, die ein Update für die CLI pushen, dann vergiss nicht, in der ```pubspec.yaml``` die Versionsnummer je nach Change (breaking, major, minor) zu ändern und anschließend ```dart run build_runner build``` laufen zu lassen, damit sich ```lib/src/version.dart``` automagisch aktualisiert!

#### Wie man eine neue Option zur CLI hinzufügt

Siehe [hier][add_option_to_cli_link].

### Achtung

Bitte bei jedem Update auch die [Doku][cli_docs_repo_link] anpassen!

[cli_docs_repo_link]: https://github.com/julien-neon/NEON_cli_docs
[template_project_link]: https://github.com/NEON-Software-Solutions/NEON_template_project
[flow_builder_link]: https://pub.dev/packages/flow_builder
[add_option_to_cli_link]: https://docs.page/julien-neon/NEON_cli_docs/add_new_brick
